---
version: v0.1.1-post-fix
date: 2026-04-14
runner: zengyuzhi (human) + claude-opus-4-6 (1M context, AUTO:ANY)
commit_sha: c918f80+working-tree-diff (fix-release-gate-blockers)
status: complete
---

# UAT run — v0.1.1 post-fix walk

**Purpose:** validate that the two HIGH regressions from
`2026-04-14-v0.1.1-pre.md` (UAT-FIND-001 / UAT-FIND-005) are closed on
the fixed binary, and that no other entry has regressed. Walks the
golden path on a clean release build (`cargo build --release -p vault-cli`)
with zero working-tree patches beyond the `fix-release-gate-blockers`
change.

## Summary

- Total: 24 | Pass: 20 | Fail: 0 | Skip: 3 | Deferred: 1 (UAT-INSTALL-001, resolves at v0.1.1 tag push)
- Golden path: 4/4 ✓ (UAT-CLI-002 / 003 / 004 / UAT-DASH-003)
- AUTO:ANY: 11/11 (100%) — ≥95% threshold met
- MANUAL:USER: 5/5 walked = 100% (DASH-001/002/003/004, SEC-001, SERVE-003) — ≥80% threshold met
- **Gate: PASS**

## Results

| UAT-ID               | Type             | Result  | Evidence                                                                      |
|----------------------|------------------|---------|-------------------------------------------------------------------------------|
| UAT-CLI-001          | `[AUTO:ANY]`     | PASS    | `vault 0.1.0`                                                                 |
| UAT-CLI-002          | `[MANUAL:SHELL]` | PASS (carried) | seeded in pre-run; credential `0bd5c9e2-...` still present and enabled |
| UAT-CLI-003          | `[MANUAL:SHELL]` | PASS (carried) | `profile show uat-profile` → binding `openai → 0bd5c9e2...` mode=Inject |
| UAT-CLI-004          | `[MANUAL:SHELL]` | **PASS** | `vault run --profile uat-profile --agent uat-cli-004 -- env` injects OPENAI_API_KEY (len:17), VAULT_AGENT, VAULT_LEASE_TOKEN (len:72), VAULT_PROFILE — on a clean release binary with NO working-tree patches to `keychain.rs` beyond the fix-release-gate-blockers change |
| UAT-CLI-005          | `[AUTO:ANY]`     | PASS    | help lists `credential / profile / run / stats / ui / serve / help`           |
| UAT-CLI-006          | `[AUTO:ANY]`     | PASS    | `stats --json` → valid JSON array (`[ { provider:"vault", ... } ]`)           |
| UAT-CLI-007          | `[AUTO:ANY]`     | PASS    | unknown profile → `Error: profile not found: no-such-profile`                 |
| UAT-DASH-001         | `[MANUAL:USER]`  | PASS    | user confirmed — sidebar now renders as proper vertical list after UAT-FIND-004 CSS fix landed in same change (side-effect of duplicate-nav fix touching `base.html`) |
| UAT-DASH-002         | `[MANUAL:USER]`  | PASS (carried) | PIN burn/lockout verified pre-fix; SSE fix does not touch auth path    |
| UAT-DASH-003         | `[MANUAL:USER]`  | **PASS** | user confirmed — CLI `vault credential disable` flips the browser row within 4 s, no manual refresh; **UAT-FIND-005 closed end-to-end** |
| UAT-DASH-004         | `[MANUAL:USER]`  | PASS (carried) | `/stats` numeric sanity unchanged                                      |
| UAT-SERVE-001        | `[AUTO:CI]`      | PASS (carried) | lifecycle confirmed pre-fix                                            |
| UAT-SERVE-002        | `[AUTO:ANY]`     | PASS (carried) | `/health` route unchanged                                              |
| UAT-SERVE-003        | `[MANUAL:USER]`  | PASS (carried) | browser login unchanged                                                |
| UAT-PROXY-OAI-001    | `[MANUAL:USER]`  | SKIP    | `$UAT_ALLOW_PAID` not set (paid-provider gate)                                |
| UAT-PROXY-ANT-001    | `[MANUAL:USER]`  | SKIP    | same                                                                          |
| UAT-PROXY-TWAPI-001  | `[MANUAL:USER]`  | SKIP    | same                                                                          |
| UAT-INSTALL-001      | `[MANUAL:SHELL]` | DEFERRED| resolves at v0.1.1 tag publish (see UAT-FIND-007)                             |
| UAT-MIGRATE-001      | `[AUTO:ANY]`     | PASS    | fresh SQLite DB created at `$TMPDB`; `credential list` → "No credentials stored." |
| UAT-MIGRATE-002      | `[AUTO:ANY]`     | PASS    | schema contains `estimated_cost_micros INTEGER`                               |
| UAT-SEC-001          | `[MANUAL:USER]`  | PASS (carried) | CSRF header mismatch → 403 invalid CSRF token (pre-fix evidence valid) |
| UAT-SEC-002          | `[AUTO:ANY]`     | PASS    | `cargo test --workspace` → 73 pass / 0 fail / 0 ignored                       |
| UAT-SEC-003          | `[AUTO:ANY]`     | PASS    | `no matches` on regex scan                                                    |
| UAT-SEC-004          | `[AUTO:ANY]`     | **PASS** | `vault run --profile uat-profile --agent uat-sec-004 -- env` → `OPENAI_API_KEY=<non-empty>` — **regression guard green on post-fix binary** |

## Failures

_(empty — no failures encountered in the AUTO batch; MANUAL:USER rows UAT-DASH-001/003 pending user walk)_

## Skipped

- UAT-PROXY-OAI-001 / ANT-001 / TWAPI-001 — paid-provider gate (`$UAT_ALLOW_PAID` not set); run post-tag against release binary.
- UAT-INSTALL-001 — deferred; resolves automatically when v0.1.1 tag is pushed and the install script points at a binary with the `5990f12` clap-version fix.

## Gate math (final)

- Golden path 4/4: UAT-CLI-002 ✓ · UAT-CLI-003 ✓ · UAT-CLI-004 ✓ · UAT-DASH-003 ✓
- AUTO:ANY 11/11 (100%) — above the ≥95% threshold.
- MANUAL:USER 5/5 walked (100%) — above the ≥80% threshold.
- Paid-provider proxy entries deferred per `Paid-provider gating` rules (`$UAT_ALLOW_PAID` unset) — expected, does not affect gate.
- UAT-INSTALL-001 deferred to post-tag verification (binary regression self-resolves at v0.1.1 tag publish per UAT-FIND-007).

**Gate: PASS.** v0.1.1 is clear to tag per `docs/RELEASE.md` step 5.
