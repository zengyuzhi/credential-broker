---
version: v0.1.1-pre
date: 2026-04-14
runner: claude-opus-4-6 (1M context)
commit_sha: 993c3a5
status: baseline-only
---

# UAT Run Log — v0.1.1-pre (AI baseline)

**Purpose:** first-ever UAT run under the `uat-release-gate` capability. Walked every
`[AUTO:ANY]` entry live; marked all `[AUTO:CI]` and `[MANUAL:*]` entries as `SKIP` per
the AI-agent runbook (see `docs/UAT.md` §Running as an AI agent). This run is NOT tied
to a production tag cut — it establishes the template and baseline against which
future per-release runs will be compared. A follow-up human-run UAT is required before
`v0.1.1` (or any subsequent tag) ships.

## Summary

- **Total:** 23 | **Pass:** 8 | **Fail:** 0 | **Skip:** 15
- **Golden path:** 1/4 (UAT-CLI-001 only; UAT-CLI-002..004 require keychain prompt, SKIP)
- **AUTO:ANY pass rate:** 8/8 non-SKIP = 100% (threshold ≥ 95%) ✓
- **MANUAL:USER pass rate:** 0/0 non-SKIP = n/a (all skipped, no denominator)
- **Gate:** **FAIL** (3 golden-path SKIPs — cannot PASS without human walk)
- **Disposition:** `status: baseline-only` — not tied to a release cut

## Results

| UAT-ID | Type | Result | Evidence |
|--------|------|--------|----------|
| UAT-CLI-001 | `[AUTO:ANY]` | PASS | stdout: `vault 0.1.0` |
| UAT-CLI-002 | `[MANUAL:SHELL]` | SKIP | AI-unsafe: keychain prompt requires human keyboard |
| UAT-CLI-003 | `[MANUAL:SHELL]` | SKIP | AI-unsafe: depends on UAT-CLI-002 keychain prompt |
| UAT-CLI-004 | `[MANUAL:SHELL]` | SKIP | AI-unsafe: depends on UAT-CLI-002/003 prior state |
| UAT-CLI-005 | `[AUTO:ANY]` | PASS | help tree lists `credential`, `profile`, `run`, `stats`, `ui`, `serve` |
| UAT-CLI-006 | `[AUTO:ANY]` | PASS | stdout: `[]` (valid JSON array, empty) |
| UAT-CLI-007 | `[AUTO:ANY]` | PASS | stdout: `error: unexpected argument '--json' found` (well-formed error, no panic) |
| UAT-DASH-001 | `[MANUAL:USER]` | SKIP | AI-unsafe: requires human eyes on browser |
| UAT-DASH-002 | `[MANUAL:USER]` | SKIP | AI-unsafe: requires human eyes on browser |
| UAT-DASH-003 | `[MANUAL:USER]` | SKIP | AI-unsafe: requires human eyes on browser |
| UAT-DASH-004 | `[MANUAL:USER]` | SKIP | AI-unsafe: requires human eyes on browser |
| UAT-SERVE-001 | `[AUTO:CI]` | SKIP | AI-unsafe: multi-step background process lifecycle spans tool calls |
| UAT-SERVE-002 | `[AUTO:ANY]` | SKIP | precondition unmet: no vault serve instance running; AI agent must not start one |
| UAT-SERVE-003 | `[MANUAL:USER]` | SKIP | AI-unsafe: requires human eyes on browser |
| UAT-PROXY-OAI-001 | `[MANUAL:USER]` | SKIP | `$UAT_ALLOW_PAID` not set |
| UAT-PROXY-ANT-001 | `[MANUAL:USER]` | SKIP | `$UAT_ALLOW_PAID` not set |
| UAT-PROXY-TWAPI-001 | `[MANUAL:USER]` | SKIP | `$UAT_ALLOW_PAID` not set |
| UAT-INSTALL-001 | `[MANUAL:SHELL]` | SKIP | AI-unsafe: scratch-shell side effects need human supervision |
| UAT-MIGRATE-001 | `[AUTO:ANY]` | PASS | stdout: `[]`; `/tmp/uat-fresh.db` created (110592 bytes) |
| UAT-MIGRATE-002 | `[AUTO:ANY]` | PASS | schema contains `estimated_cost_micros INTEGER`; does not contain `estimated_cost_usd` |
| UAT-SEC-001 | `[MANUAL:USER]` | SKIP | AI-unsafe: dashboard session-cookie rotation needs live browser |
| UAT-SEC-002 | `[AUTO:ANY]` | PASS | 73 tests passed across workspace; no `FAILED` |
| UAT-SEC-003 | `[AUTO:ANY]` | PASS | stdout: `no matches` (SE-06 guard intact) |

## Failures

None. No entry reported `FAIL`.

## Skipped

All 15 skips fall into one of three documented categories:

1. **AI-unsafe (`[AUTO:CI]` or `[MANUAL:*]`):** 12 entries — require the user's keyboard,
   eyes, or a shell runner that can manage multi-step state across tool calls.
   Human-run UAT is required before tag push.
2. **Unmet precondition:** 1 entry (UAT-SERVE-002) — server was not running; AI agent
   must not start background processes per the runbook.
3. **Paid-provider gate:** 3 entries (`UAT-PROXY-*`) — `$UAT_ALLOW_PAID` not set;
   default-skip per spec.

## Follow-up actions

- **Blocker for v0.1.1 tag:** human maintainer must run UAT-CLI-002/003/004 (golden path)
  and UAT-DASH-001/002/003/004 to bring `Gate: FAIL` → `PASS` or explicitly re-dispose
  failures via spec update.
- **UAT-CLI-007 pass-criteria nuance:** `vault credential list --json` is not yet
  implemented — the entry passed because the CLI surfaced a well-formed `error:` line
  rather than a panic. Decide for v0.1.1 whether to implement `--json` or tighten the
  regex to demand JSON-only once the flag lands.
- **UAT-SERVE-002:** future shell-runner variant should bundle start-server → curl →
  stop-server into one `[AUTO:CI]` block so the check is exercised regularly.

## Notes

- Commit at run time: `993c3a5` (after `CLAUDE.md` hardening-gotchas update and before
  the UAT-gate commit itself lands).
- Runner was an AI agent (Claude Opus 4.6 in 1M-context mode); filename carries the
  `-ai` suffix per convention.
- Evidence lines above are reproduced literally from command stdout; no secrets,
  tokens, PINs, or UUIDs were emitted by the AUTO:ANY entries, so no redaction was
  needed.
